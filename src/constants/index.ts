// PC 设备指定宽度
export const PC_DEVICE_WIDTH: number = 1280

export interface Rate {
  class: string
  counting: string
  notclock: string
  rate: number
}

export interface Detail {
  id: string
  name: string
  grade: string
  class: string
  progress: string
}
